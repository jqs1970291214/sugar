spring:
  application:
    name: provider
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: provider
    config:
      uri: http://localhost:9999/
      profile: default
      label: master
server:
  port: 8000


